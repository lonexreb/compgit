# 2026-04-20

## Phase 0 shipped

- Monorepo scaffolded under `consumer-application/compgit/`. pnpm workspaces + Turborepo + Biome.
- [[../entities/Schema-As-Source-Of-Truth]] live. `tools/schemagen` emits TS + Swift from `compgit.schema.json`.
- Shared Swift package (`CompgitSchema`, `CompgitCore`) builds on iOS 17+ / macOS 14+.
- CI workflow: TS typecheck + Biome + schema drift check on Ubuntu, `swift build` on macos-14.

## Decisions

- [[../decisions/2026-04-20-pat-first-auth]] — PAT paste, no backend in v1.
- [[../decisions/2026-04-20-merged-schema]] — single schema file with `definitions`.
- [[../decisions/2026-04-20-hand-rolled-schemagen]] — drop quicktype.
- [[../decisions/2026-04-20-no-cal-heatmap]] — SVG charts only.

## Verification

- `pnpm install && pnpm gen:schema && pnpm typecheck && pnpm lint` all green.
- `swift build` at `packages/shared-swift/` completes in ~20s cold, 0.1s warm.
- Drift check round-trips cleanly.
