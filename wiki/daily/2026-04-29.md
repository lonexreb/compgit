# 2026-04-29

## End-to-end plan expansion

Lifted the plan from "next-tasks list" to "every remaining phase has its own entity page with verification gates". Added a Phase 6 the original plan elided.

New entity pages:

- [[../entities/Apple-Surfaces]] — Phase 2b (iOS + macOS apps + widget extensions).
- [[../entities/Compare-Surface]] — Phase 3 (follow + compare).
- [[../entities/OAuth-Worker]] — Phase 4 (Cloudflare Worker + OAuth + KV).
- [[../entities/Distribution-And-Release]] — Phase 5 (every storefront + sideload).
- [[../entities/Telemetry-And-Support]] — Phase 6 (post-launch loop).

## Decisions

- [[../decisions/2026-04-29-end-to-end-plan-expansion]] — recorded the choice + alternatives + revisit conditions.

## Doc updates

- `wiki/Home.md` roadmap split Phase 2 into 2a/2b, added Phase 6, and points each row at its entity.
- `wiki/decisions/README.md` indexes the new decision.
- `NEXT-TO-DO.md` rewritten end-to-end: prerequisites, scope, per-task `Verify:` lines, per-phase exit gate, time estimates. Added a Pre-Phase-2b prep section that runs without Xcode (XcodeGen `project.yml`, `Tokens.swift` mirror of `tokens.css`, `baseURL` arg on `GitHubClient` for Phase 4, icon swap).
- `MEMORY.md` gains atomic facts pointing at the new entities + Phase 6.

## Why now

The gap from "Phase 4 — Cloudflare Worker + OAuth" (one bullet) to actually building it was hiding an OAuth + PKCE flow, KV layout, session model, two-surface migration, and rate-limit math. Future-me would have re-derived all of it. Same for Phase 5's storefront matrix and Phase 6 (which didn't exist).

## Next

Pick up the **Pre-Phase-2b prep** tasks in `NEXT-TO-DO.md` — they unblock Phase 2b without Xcode. Specifically:

1. Add XcodeGen `project.yml` skeletons to `apps/ios/` and `apps/macos/`.
2. Sketch `Tokens.swift` from `apps/chrome/styles/tokens.css`.
3. Add `baseURL` to `GitHubClient` (TS + Swift) for the Phase-4 flip.
4. Replace the WXT placeholder icon.

Each lands as its own conventional commit and pushes immediately. None require Xcode or the Apple Developer Program.
